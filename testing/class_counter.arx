using io

class Counter {
    int value = 0

    void _init(int start) {
        this.value = start
    }

    void increment() {
        this.value++
    }
}

int _exec() {
    any:Counter c = Counter(69)

    c.increment()
    io.print(c.value)

    return 0
}
